@{
    var showcase = await _showcaseService.GetLatest();
}

<section class="showcase">
    <div class="container">
        <div class="content">
            <div class="ingress">@showcase.Ingress</div>
            <div class="title">@showcase.Title</div>
            <button class="btn-theme">@showcase!.Link!.Text</button>
        </div>
        <div class="content-image">
            <img src="@showcase.ImageUrl" alt="@showcase.Title">
        </div>
    </div>
</section>